package com.skybooking.config;

/**
 * H2DataInitializer removed — DB seeding has been removed from the project per user request.
 * If you want to seed data for local testing, run the SQL scripts manually in your DB client (DBeaver),
 * or add a separate tooling step outside the application lifecycle.
 */
public class H2DataInitializerRemoved {
    // Intentionally left blank to avoid automatic DB seeding.
}
