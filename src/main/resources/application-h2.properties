# Removed: application-h2.properties (H2 profile removed per request). Configure your database connection in your local application.properties or pass runtime properties when starting the app.
# If you need example properties, use the MySQL configuration in README or add your own local profile.